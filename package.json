{
  "name": "learning-playwright",
  "version": "1.0.0",
  "description": "Playwright learning examples",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "install-browsers": "npx playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.0"
  }
}
